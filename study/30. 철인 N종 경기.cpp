#include <iostream>
using namespace std;

int main()
{
	ios_base::sync_with_stdio(false);
	cin.tie(nullptr);
	cout.tie(nullptr);

	// m: 종목의 수

	int C;
	cin >> C;
	while (C--) {
		int m;
		cin >> m;
		for (int i = 0; i < m; ++i) {
			int a, b;
			cin >> a >> b;
		}
	}
	system("pause");
	return 0;
}